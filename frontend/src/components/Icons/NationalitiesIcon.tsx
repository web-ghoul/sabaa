import { FlagRounded } from "@mui/icons-material";

const NationalitiesIcon = ({ classes }: { classes?: string }) => {
  return <FlagRounded className={classes} />;
};

export default NationalitiesIcon;
