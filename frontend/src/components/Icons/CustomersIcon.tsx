import { Diversity3Rounded } from "@mui/icons-material";

const CustomersIcon = ({ classes }: { classes?: string }) => {
  return <Diversity3Rounded className={classes} />;
};

export default CustomersIcon;
