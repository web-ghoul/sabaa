import { CreditCardRounded } from "@mui/icons-material";

const EChannelsIcon = ({ classes }: { classes?: string }) => {
  return <CreditCardRounded className={classes} />;
};

export default EChannelsIcon;
