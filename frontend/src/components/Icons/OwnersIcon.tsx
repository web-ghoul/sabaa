import { AssignmentIndRounded } from "@mui/icons-material";

const OwnersIcon = ({ classes }: { classes?: string }) => {
  return <AssignmentIndRounded className={classes} />;
};

export default OwnersIcon;
